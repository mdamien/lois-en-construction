cp texte_00.md git/texte
git -C git add texte
git -C git commit -m 'Texte du projet de loi'
cp texte_01.md git/texte
git -C git add texte
git -C git commit -m 'Texte adopté par la commission de l’Assemblée nationale en première lecture'
cp texte_02.md git/texte
git -C git add texte
git -C git commit -m 'Texte adopté par l’Assemblée nationale en première lecture'
cp texte_03.md git/texte
git -C git add texte
git -C git commit -m 'Texte adopté par la commission du Sénat en première lecture'
cp texte_04.md git/texte
git -C git add texte
git -C git commit -m 'Texte adopté par le Sénat en première lecture'
cp texte_05.md git/texte
git -C git add texte
git -C git commit -m 'Texte adopté en commission mixte paritaire'
cp texte_06.md git/texte
git -C git add texte
git -C git commit -m 'Texte définitif établi au Sénat'
cp texte_07.md git/texte
git -C git add texte
git -C git commit -m 'Texte promulgué'
